
<!-- Using Blacklight Dropdown -->

<!--
<div id="sort-dropdown" class="dropdown pull-right hidden-phone">
  <ul class="css-dropdown">
    <li class="btn btn-link">
      <%= link_to("Sort by #{current_sort_field ? current_sort_field.label : 'default'}".html_safe, "#") %> <span class="caret"></span>
      <ul>
        <%- blacklight_config.sort_fields.each do |sort_key, field| %>
          <li><%= link_to(field.label, url_for(params_for_search.merge(:sort => sort_key))) %></li>
        <%- end -%>
      </ul>
    </li>
  </ul>
</div>
-->

<!-- Using Bootstrap Dropdowns -->
<li id="sort-dropdown" class="dropdown hidden-phone">

  <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= "Sort by #{current_sort_field.label}" %><b class="caret"></b></a>

  <ul class="dropdown-menu">

  <%- blacklight_config.sort_fields.each do |sort_key, field| %>
    <li><%= link_to(field.label, url_for(params_for_search.merge(:sort => sort_key))) %></li>
  <%- end -%>

  </ul>
</li>
