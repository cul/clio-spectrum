- if session[:search][:counter]
  #appliedParams
    .yui-gc
      .yui-u.first
        %h3.label You searched for:
        = render_constraints(localized_params)
      .yui-u

        .back_to_catalog= link_back_to_catalog
        =item_page_entry_info.gsub(" from your search.","")

        =render "show_previous_next_doc"
