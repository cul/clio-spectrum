.results_header
  .category_title 
    #{title}
    - if defined?(description)
      %a.source-info.btn.btn-link{:href => '#', :rel => 'popover', :'data-placement' => 'bottom', :'data-content' => description}
        = image_tag('information_icon.png', :size => "20x20")
  - if result[:count].to_i > 0
    .result_count
      = link_to "All <span class='visible-desktop'>#{number_with_delimiter(result[:count])}</span> results".html_safe, result[:url]
