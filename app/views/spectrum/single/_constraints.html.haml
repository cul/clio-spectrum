
-# GOOGLE APPLIANCE - CONSTRAINTS DISPLAY

.well.well-constraints
  %span.constraints-label You searched for:

  = link_to "Start Over", result.start_over_link, :class => "single_startOverLink", :id => "startOverLink"
  - result.constraints_with_links.each do |text, link|

    %div.constraint-box
      %span.filterValue
        =text
        =link_to content_tag(:i, '', :class => 'icon-remove'), link
      %span.chrome-fix
