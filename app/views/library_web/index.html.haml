
UNUSED
UNUSED
UNUSED
UNUSED
UNUSED
UNUSED
UNUSED
UNUSED
UNUSED

- @page_title = application_name + " Library Web Search"
- if @results

  .pagination_container
    = render :partial => '/_search/pagination', :locals => {:results => @results}

  #documents
    = render_documents(@results.docs, :action => "library_web", :viewstyle => (params[:view] || "list"))
- else

  = add_all_datasource_landing_pages
