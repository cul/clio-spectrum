- @page_title = application_name + " Library Web Search"
- if @results
  .pagination_container
    =render :partial => '/_search/pagination'

  = render :partial => 'document_list', :locals => {:search => @result.docs }
