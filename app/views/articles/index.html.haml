- @page_title = application_name + " Article Search"

-#= render :partial => 'search_form' %>
	
-#= render :partial => 'did_you_mean' %>

= render '/_search/constraints', :localized_params=>params 
  
-#.pageEntriesInfo
  -#= render "change_view" 
  -#= render_pagination_info @response, :entry_name=>'item'
  -#= link_to(image_tag("rss-feed-icon-14x14.png"), catalog_index_path(params.merge(:format => "atom")).gsub('[','%5B').gsub(']','%5D'),:id => "feedLink") 
  -#=link_to "start over", catalog_index_path, :id=>"startOverLink" 
  
-#= render 'sort_and_per_page'
-#.pagination_container
  -#= render 'results_pagination' 

.landing_text
  %h1 Article Search

  This is the article search for CLIO beta. It is powered by Summon.

- sidebar_items << capture do 
  = render :partial => "/_search/facets", :locals => {:facets => @summon.search.facets}

-#.pagination_container

  -#= render 'results_pagination' %>



