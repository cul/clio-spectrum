

UNUSED
UNUSED
UNUSED
UNUSED
UNUSED
UNUSED


- @page_title = application_name + " Search Results"

- if @error
  #intro_text
    %h2 Article Search
    %p This search has returned an error. Right now, the Summon API is having trouble with unicode searches. We are currently working on this with Serial Solutions. If you did not do a search involving unicode, please use the feedback button to report your problem

    %p Error: #{@error}

- else
  -#= render 'search_form' %>

  -#= render 'did_you_mean' %>

  = render '/_search/constraints', :localized_params => params

  -#.pageEntriesInfo
    -#= render "change_view"
    -#= render_pagination_info @response, :entry_name=>'item'
    -#= link_to(image_tag("rss-feed-icon-14x14.png"), catalog_index_path(params.merge(:format => "atom")).gsub('[','%5B').gsub(']','%5D'),:id => "feedLink")
    -#=link_to "start over", catalog_index_path, :id=>"startOverLink"

  -#= render 'sort_and_per_page'
  -#.pagination_container
    -#= render 'results_pagination'

  .pagination_container
    = render '/_search/pagination', :results => @summon

  = render 'document_list', :search => @summon.search

  .pagination_container
    = render '/_search/pagination', :results => @summon

  - clio_sidebar_items << capture do
    = render "/_search/facets", :facets => @summon.search.facets




