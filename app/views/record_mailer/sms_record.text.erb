<% @documents.each do |document| %>
  <%= t('blacklight.sms.text.url', :url => polymorphic_url(document, @url_gen_params) ) %>
  <%= document.to_sms_text %>
  <%= document.call_numbers %>
<% end %>
