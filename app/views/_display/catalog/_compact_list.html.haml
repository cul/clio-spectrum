
-# catalog search-results listing, COMPACT list format

/ _display/catalog/_compact_list.html.haml

- document_list.each_with_index do |document,counter|
  - blacklight_counter = counter + 1 + @response.params[:start].to_i

  = render_document_view(document, :format => 'clio', :template => 'compact_list_item', :counter => blacklight_counter)

