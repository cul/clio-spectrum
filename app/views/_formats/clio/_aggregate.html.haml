.result{class: alternating_line("catalog"), source: 'catalog', item_id: document.get(:id), google_ids: extract_google_bibkeys(document)}
  %abbr.unapi-id{:title => document.get(:id)}
  .result_title= link_to document_full_title(document), {:controller => 'catalog', :action => 'show', :id => document.id, :counter => (document_counter + 1)}
  = render :partial => "/_formats/clio/catalog_list_details", :locals => {:document => document}

