  quit
where
marc
continue
marc
continue
params[:forgiving]        
      num_fields = directory.length / DIRECTORY_ENTRY_LENGTH
      raise MARC::Exception.new("invalid directory in record") if directory == nil
      directory = marc[LEADER_LENGTH..base_address-1]
marc[LEADER_LENGTH..base_address-1]
      base_address = record.leader[12..16].to_i
      record.leader = marc[0..LEADER_LENGTH-1]
LEADER_LENGTH
      record = Record.new()
marc.force_encoding("binary")
marc.respond_to?(:force_encoding)
        params[:external_encoding] = marc.encoding
marc.encoding
marc.respond_to?(:encoding)
params.has_key?(:encoding)
